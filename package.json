{
  "name": "geoflow",
  "version": "1.0.0",
  "description": "GeoFlow — unified map-based travel discovery platform (frontend + backend)",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend start",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend start\" \"npm --prefix frontend start\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
